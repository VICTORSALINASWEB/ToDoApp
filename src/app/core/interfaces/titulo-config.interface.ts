/* =========================================================
   src/app/core/interfaces/header-config.interface.ts
========================================================= */
 

export interface TituloConfig {
  // ── Contenido ────────────────────────────────────────────
  title:        string;
  subtitle?:    string;
  mostrarRefresh?: boolean;
}